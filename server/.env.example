# Server - Thi trắc nghiệm (mota.md)
# Dev: chạy local. Production: sửa IP/PORT rồi build Client trỏ về đây.

PORT=3001
HOST=0.0.0.0

# SQLite (file local, không cần cài DB server)
DATABASE_PATH=./data/quiz.db

# Thư mục ngân hàng đề (import từ JSON/AIKEN)
QUESTION_BANK_PATH=../question-bank
